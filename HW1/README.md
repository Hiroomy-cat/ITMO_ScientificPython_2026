## Homework 1 (HW1)

### Learn Git Branching Screenshots

Screenshots of your progress through the Learn Git Branching game are saved in the `HW1/` folder as text files (base64):
1_part.txt
2_part.txt

### How to convert text files back to images:

```bash
# To convert base64 files back to PNG, use the command:
base64 -d HW1/1_part.txt > 1_part.png
base64 -d HW1/2_part.txt > 2_part.png
